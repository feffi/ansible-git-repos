---
galaxy_info:
  author: Kevin Wennemuth
  description: Ansible role to manage git repositories.
  license: MIT
  min_ansible_version: 2.7.1.0
  platforms:
    - name: Darwin
      versions:
        - all
  galaxy_tags:
    - system
    - config
    - git
dependencies:
  - { role: ansible-pacman,
        ansible_pacman: {
        install: [
          "git"
        ]
      }
    }
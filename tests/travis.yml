---
- hosts: 127.0.0.1
  connection: local
  vars:
    DEBUG: true
    macos_git_repos:
      containment: "{{ ansible_env.HOME + '/workspace' }}"
      repositories: []
  roles:
    - { role: ansible-macos-git-repos }
---
- hosts: 127.0.0.1
  connection: local
  vars:
    DEBUG: true
    git_repos:
      containment: "{{ ansible_env.HOME + '/workspace' }}"
      repositories: []
  roles:
    - { role: ansible-git-repos }